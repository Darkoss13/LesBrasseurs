<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">
<!-- Pour écrire et lire dans le fichier txt, il faut donner l'autorisation pour la lecture sur les fichiers externes !-->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Distributeur" />
        <activity android:name=".Grossiste" />
        <activity android:name=".Detaillant" />
        <activity android:name=".Brasseur" />
        <activity android:name=".RejoindrePartie" />
        <activity android:name=".PageConnexionAttenteJoueur" />
        <activity android:name=".CreerPartie" />
        <activity android:name=".Instruction" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>